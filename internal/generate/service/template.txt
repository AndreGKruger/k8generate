apiVersion: v1
kind: Service
metadata:
  name: {{ .Appname }}-svc
  namespace: {{ .Namespace }}
  labels:
    app: {{ .Appname }}-svc
spec:
  type: NodePort
  ports:
    - port: 80
  selector:
    app: {{ .Appname }}